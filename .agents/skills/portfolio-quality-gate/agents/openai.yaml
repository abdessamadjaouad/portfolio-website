interface:
  display_name: "Portfolio Quality Gate"
  short_description: "Audit portfolio phase readiness without changing files"
  default_prompt: "Use $portfolio-quality-gate to audit the active portfolio phase and classify every check as Blocker, Required, Optional, or Passed."
